# 更新日志 (CHANGELOG)

## [v1.4.7] - 登录结果明示当前账号、识别游客/登错账号

*   **✨ 【新特性】登录结果显示当前登录账号**：扫码登录成功后的提示会附带「当前登录账号：xx***xx」，一眼确认登的是不是自己要的账号。
*   **✨ 【新特性】异常账号识别**：已登录但 `/user/detail` 返回 404（网易云用户系统查无此人）时，判定为游客/异常账号，登录提示与日志都会明确告知「极可能登错账号」，并引导用户在 App「我的」页面确认当前账号后重新扫码。
*   **🛠️ 【增强】诊断日志扩充**：`登录复核完成(v1.4.7)` 增加 `userName` 与 `user/detail.code` 字段，排查登录问题所需信息一次给全。

## [v1.4.6] - VIP 判定三来源取最大 + 登录复核诊断日志

*   **🐛 【修复】部分账号 v1.4.5 仍被误判为非 VIP**：
    *   登录复核的 VIP 判定改为三来源取最大值：`/login/status` 的 `account.vipType`（账号对象自带，此前漏读）、`profile.vipType`、`/user/detail` 完整资料的 `vipType`，任一来源大于 0 即判定为 VIP。
*   **✨ 【新增】登录复核诊断日志**：每次复核输出 `登录复核完成(v1.4.6)：uid=…, account.vipType=…, profile.vipType=…, user/detail.vipType=…, 最终判定=…`，日志中各来源的原始值一目了然，也便于确认新版本是否已生效。

## [v1.4.5] - 修复 SVIP 账号被误判为非 VIP

*   **🐛 【修复】黑胶 VIP/SVIP 账号登录后被提示「该账号不是 VIP」**：
    *   根因：`/login/status` 底层走 `/api/w/nuser/account/get` 轻量接口，其 profile 经常不携带 `vipType` 字段，SVIP 账号也会被判定为非会员，导致会员歌曲被错误地降级到 Meting 镜像、播放卡片显示灰框。
    *   修复：登录复核拿不到 `vipType` 时，自动调用 `/user/detail?uid=` 获取完整用户资料二次确认会员身份；两处任一返回 `vipType>0` 即判定为 VIP。

## [v1.4.4] - 播放卡片 VIP 标记：红框 = VIP 账号下载，灰框 = 镜像降级

*   **✨ 【新特性】播放卡片 VIP 徽标**：VIP 歌曲（接口 fee=1/4 或检出试听降级）在播放卡片上显示「VIP · 音质」胶囊——
    *   **红色框**：VIP 歌曲通过已登录的 VIP 账号下载（如 `VIP · 无损 FLAC`）；
    *   **灰色框**：非 VIP 账号 / 登录失效，经 Meting 镜像等降级下载（如 `VIP · Meting 镜像`）；
    *   普通免费歌曲保持原「♪ 音质」红色胶囊不变。

## [v1.4.3] - 试听片段识别、登录失效点歌提示、自动降级 Meting 镜像

*   **🐛 【修复】登录后 VIP 歌曲仍只下载/发送 30 秒试听部分，且日志无任何提示**：
    *   根因：`/song/url/v1` 在 cookie 失效、未登录或账号非 VIP 时**仍会返回 URL，但那是带 `freeTrialInfo` 标记的试听片段**，旧版只判断 URL 非空就当成功；而启动时的 `/login/status` 校验可能通过一次后 cookie 又悄悄失效，故障全程无感知。
    *   现在识别 `freeTrialInfo` 标记：任何音源返回试听片段都**不再当作成功**，立即复核登录态（60 秒防抖），并**自动降级到 Meting 镜像**下载完整歌曲，不再把试听片段发给用户。
*   **✨ 【新特性】点歌时登录失效提示**：插件确认登录失效后（启动校验或下载时检出试听片段），点歌会先发送「⚠️ 网易云未登录或登录已失效…可使用 /网易云登录 重新扫码」提示（10 分钟内最多一次，避免刷屏）。
*   **✨ 【增强】登录状态可感知**：
    *   启动校验区分三种状态：未登录/失效、已登录非 VIP、已登录 VIP，日志分别提示；账号非 VIP 时不再误报「会员音质可用」。
    *   扫码登录成功后立即复核会员身份，分别提示「VIP 账号，已解锁会员音质」或「该账号不是 VIP，会员歌曲将降级镜像」。
    *   登录复核网络异常时保留原状态，避免抖动误报失效。
*   点歌结果与日志会标注降级来源，如「音质 Meting 镜像（VIP/登录失效，已自动降级 Meting 镜像）」。

## [v1.4.2] - 修复 VIP 歌曲间歇性只能下载 30 秒试听

*   **🐛 【修复】登录会员后 VIP 歌曲时好时坏（偶尔只剩 30 秒试听）**：两个叠加原因——
    *   **服务端缓存不认 Cookie**：NeteaseCloudMusicApi 的 apicache 按 URL 缓存响应，若某首歌在 Cookie 未生效的瞬间被请求过，30 秒试听地址会被缓存，之后带 Cookie 请求也命中旧缓存。现 `/song/url/v1` 统一携带毫秒时间戳参数破除缓存。
    *   **CookieJar 干扰手动鉴权**：aiohttp 会话会自动保存登录接口 Set-Cookie 并与手动传入的 Cookie 头叠加，可能出现重复/过期 MUSIC_U。现改用 DummyCookieJar 完全禁用会话自动 Cookie，登录态只由插件持久化的 Cookie 文件决定。

## [v1.4.1] - 修复内置服务 Permission denied

*   **🐛 【修复】内置服务启动报 Permission denied**：zip 安装或只读挂载的插件目录中，二进制没有执行权限。现启动前统一把二进制复制到插件数据目录并赋予执行权限再运行，数据目录副本与包内文件大小一致时直接复用，不重复复制。

## [v1.4.0] - 内置服务二进制随插件打包、修复启动卡死

*   **✨ 【变更】内置服务二进制随插件打包**：
    *   官方预编译服务（linux / windows / macOS x64，MIT 许可）直接打包进插件 `bin/` 目录，开启 `ncm_api_embedded` 后开箱即用，**不再需要联网下载**；包内文件缺失时才回退到 Release 下载（保留镜像轮换与断点续传兜底）。

*   **🐛 【修复】开启内置服务后插件重载卡住、AstrBot 无法重启**：
    *   根因：`initialize()` 同步等待二进制下载与健康检查，阻塞了 AstrBot 的插件加载流程。现内置服务启动与登录状态校验全部移入后台任务，插件加载即刻返回，服务就绪后自动切换音源；插件卸载/重载时会取消后台任务并终止子进程。

## [v1.3.1] - 内置服务下载源自动轮换与断点续传

*   **🐛 【修复】内置服务下载失败（Timeout on reading data from socket）**：
    *   根因：直连 GitHub Release 下载约 70MB 二进制时网络停滞。现下载源自动轮换：用户配置镜像 → 直连 GitHub → 内置加速镜像（ghfast.top / gh-proxy.com），每个源最多试 2 次。
    *   支持 Range 断点续传：中断后重试从已下载位置继续，不再从零开始；下载过程每 10MB 打印一次进度日志。
    *   全部源失败时给出明确排查提示（配置 `ncm_api_embedded_mirror` 或 `http_proxy`）。
    *   内置服务就绪等待由 45 秒放宽至 60 秒。

## [v1.3.0] - 内置 NeteaseCloudMusicApi 服务、登录体验修复、下载稳定性修复

*   **✨ 【新特性】内置 NeteaseCloudMusicApi 服务（开箱即用的扫码登录）**：
    *   新增 `ncm_api_embedded` 开关（**默认关闭**）：开启后插件在（重）启动时自动从官方 Release 下载对应平台的预编译服务（约 70MB，MIT 许可允许分发），以子进程方式运行在本机 `127.0.0.1:13000`，无需自己部署 Node 服务即可扫码登录、解锁无损/母带音质。
    *   关闭开关即不下载、不运行任何进程；已下载的二进制保留在插件数据目录，下次开启直接使用。端口（`ncm_api_embedded_port`）与下载镜像前缀（`ncm_api_embedded_mirror`，应对 GitHub 访问困难）可配置。
    *   开启内置服务后自动优先生效，`ncm_api_base` 外部服务地址作为未开启时的备选。

*   **🐛 【修复】扫码登录长时间不提示成功**：
    *   根因：二维码相关接口固定使用 `timestamp=0`，服务端 apicache 以完整 URL 为缓存键，导致扫码状态查询被缓存，迟迟查不到「已确认」。现改为真实毫秒时间戳，轮询间隔由 3 秒缩短至 1.5 秒，确认后即时提示登录成功。
    *   新增 802 中间态提示：扫码后未确认时会收到「请在 App 上点击确认登录」。

*   **🐛 【修复】重启后疑似掉登录**：
    *   部分服务端版本只在 `Set-Cookie` 响应头返回登录 Cookie（响应体为空），旧版会保存空 Cookie。现自动从响应头兜底合并，且仅在拿到非空 Cookie 时才覆盖本地持久化文件。
    *   插件启动时自动校验本地 Cookie 有效性，失效会在日志中明确提示重新扫码。

*   **🐛 【修复】偶发下载超时失败**：
    *   下载超时由「总时长」改为「停滞超时」（sock_read）：只要数据持续到达，网速慢的大文件（无损/母带 30-100MB）不再被误杀；连接超时单独限制 10 秒。
    *   下载失败自动等待 3 秒后重试一次。

*   **🛠️ 【体验优化】音质配置中文化**：
    *   `quality` 配置项由英文 key（`exhigh` 等）改为「标准 128k / 较高 192k / 极高 320k / 无损 FLAC / 高清臻音 Hi-Res / 超清母带」中文档位名，含义一目了然；旧配置中的英文 key 自动兼容映射，无需手动修改。

## [v1.2.2] - 盘心半径减半、液态玻璃效果

*   **🎨 【视觉优化】播放卡片盘毂重做**：
    *   **盘心半径减半**：盘毂（封面中心的圆形区域）半径由盘面的 19% 缩小至 9.5%，中心孔与银色内圈同步等比缩小，盘面展示封面的面积更大。
    *   **液态玻璃效果**：盘毂由「高斯模糊 + 压暗」升级为「液态玻璃」质感——凸透镜式折射放大（取略小的封面中心区域放大）、轻薄提亮层保留封面透出的颜色、顶部锐利月牙高光 + 底部暗色月牙模拟液体厚度、整圈内阴影与上半圈亮边模拟玻璃边缘折射，通透感更强。

## [v1.2.1] - 播放卡片盘面缩小、盘心模糊化、发送结果静默化

*   **🎨 【视觉优化】播放卡片 CD 盘面比例调整**：
    *   **唱片圆形减小**：播放卡片中的大 CD 盘面缩小为原先的约 82% 并垂直居中，为歌曲信息区留出更多呼吸空间，整体构图更均衡。
    *   **盘心模糊化处理**：盘毂区域不再填充纯黑色块，改为取封面中心区域做高斯模糊并轻压暗，保留封面色彩氛围的同时维持盘毂的视觉层次（高光环、银色内圈、中心孔不变）。

*   **🛠️ 【体验优化】发送结果解释静默化**：
    *   点歌完成后的「✅ 《xx》- xx，音质 xx，以「语音」方式发送」结果说明不再作为消息发送到聊天中，改为打印到 AstrBot 运行日志（`[ncm_player]` 前缀），聊天窗口更干净；排查发送方式时查看日志即可。

## [v1.2.0] - 关键词监听点歌、LLM 工具描述强化、纯数字序号快捷播放

*   **✨ 【新特性】关键词监听点歌（LLM 失效兜底通道）**：
    *   **口语化触发直接搜歌**：新增消息监听器，命中「我想听 / 我要听 / 点歌 / 来一首 / 来首 / 放一首 / 放首 / 播放」等触发词时，不经过 LLM 直接完成搜歌并发送选歌图，彻底解决群聊中 LLM 不调用工具导致点歌无响应的问题。
    *   **防误触发双重门禁**：触发词必须出现在消息开头附近（前 6 个字符内），且后方必须跟随有效歌名（支持《》书名号提取），闲聊中无意提到"想听听你的意见"这类句子不会打扰；提供 `enable_keyword_listen` 开关可随时关闭。
    *   **纯数字序号快捷播放**：选歌图发出后 5 分钟内，回复纯数字（如 `1`、`2`）即被识别为选歌序号直接播放，无需 LLM 参与；非待选期间的数字消息原样放行，不影响正常对话。

*   **🛠️ 【优化】LLM 工具描述强化**：
    *   **触发场景显式声明**：重写 `search_songs` 与 `play_song` 的工具描述（docstring），逐条列举"我想听xx""点歌""来一首xx""播放xx"等典型触发话术，并明确要求模型在点歌意图下必须调用工具而非仅文字回复，显著提升群聊场景的工具命中率。
    *   **两工具职责边界清晰化**：明确"歌名明确 → 直接 play_song；描述模糊/需要挑选 → search_songs 后由用户报序号"的调用分工，减少模型在两工具之间的犹豫与误用。

*   **📕 【文档】新增本更新日志**，记录各版本变更。

## [v1.1.0] - CD 美学渲染、音质档位化、扫码登录、热评卡片与歌词合并转发

*   **🎨 【视觉重构】CD 光盘美学渲染风格**：
    *   **盘面级封面渲染**：封面图渲染为带彩虹光泽色带的 CD 光盘（黑色盘毂 + 银色内圈 + 中心孔 + 外缘高光），选歌列表每行一张小 CD，播放卡片为大 CD 配柔和投影。
    *   **浅色磨砂底设计**：选歌图、播放卡片、热评卡片统一改为浅色磨砂渐变底 + 白色圆角卡体 + 网易云红点缀，告别深色卡片风。
    *   **热评卡片**：点歌后自动嗅探点赞最高的一条热评，渲染为引号卡片（圆形头像 + 昵称 + 点赞数）随歌曲发送，可用 `send_comment_card` 关闭。
    *   **字体缺字形防护**：引号等易缺字形符号改为手绘图形，杜绝豆腐块。

*   **✨ 【新特性】音质档位化与扫码登录**：
    *   **六档音质预设**：`quality` 从裸码率改为 `standard`（标准 128k）/ `higher`（较高 192k）/ `exhigh`（极高 320k）/ `lossless`（无损 FLAC）/ `hires`（高清臻音）/ `jymaster`（超清母带）六档，取不到自动逐级回退。
    *   **二维码登录**：新增 `/网易云登录` 指令，生成二维码由网易云音乐 App 扫码登录，Cookie 持久化保存，登录后解锁无损 / Hi-Res / 母带等会员音质（需配置 `ncm_api_base`）。

*   **✨ 【新特性】歌词合并转发**：
    *   点歌后嗅探整首歌词，自动剥离 LRC 时间轴标签，以 QQ 合并转发（聊天记录）形式发送，每 30 行一条节点消息；纯音乐无歌词时自动静默跳过，可用 `send_lyrics_forward` 关闭。

*   **✨ 【新特性】发送方式自由组合**：
    *   `send_mode` 支持 `auto`（语音优先自动降级）/ `voice`（仅语音）/ `file`（仅文件）/ `card`（仅卡片）及 `voice_card`、`voice_file`、`file_card`、`voice_file_card` 等下划线组合同时多发；所选方式全部失败时以歌曲链接保底。

*   **🛠️ 【音源扩展】官方网页接口 + Meting 镜像双轨**：
    *   播放地址获取升级为四级链路：自建 NeteaseCloudMusicApi（音质可控）→ 网易云官方网页接口 → Meting 镜像 → 官方外链，任一环节可用即出歌。

## [v1.0.0] - 初始版本

*   **🎉 发布**：插件初始版本发布。
*   **✨ 功能列表**：
    *   LLM 自然语言点歌（`search_songs` / `play_song` 双工具）与 `/点歌`、`/直接点歌` 指令。
    *   网易云搜索 + 封面选歌图渲染（封面、歌名、歌手、专辑、时长）。
    *   语音 → 文件 → 音乐卡片 → 链接 三级降级发送，每级独立超时。
    *   载入方式可选：本地文件路径（file）/ 直链（url）/ 编码内嵌（base64），file 模式绕开 AstrBot 强制 base64 转码，根治大文件撑爆 WebSocket（Max payload size exceeded）导致的点歌 60 秒超时。
    *   音质配置（128k-无损）、下载大小上限、单级发送超时可调。
